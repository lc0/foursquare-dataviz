machine:
  services:
    - docker

test:
  override:
    - make build

deployment:
  production:
    branch: master
    commands:
      - curl -u $DOCKER_USER:$DOCKER_API https://cloud.docker.com/api/app/v1/service/
      - make push

      # - deploy/deploy_shinyapps
